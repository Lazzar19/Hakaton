[project]
name = "kompozicija-ulaza"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.10.0"
dependencies = [
    "metadrive-simulator>=0.4.3",
]
